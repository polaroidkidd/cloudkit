<section class="mb-20">
	<h3 class="h3 text-center mb-5">Prerequisites</h3>
	<div class="text-xl text-center">
		Becase <code>PR.yaml</code> action uses docker containers, you'll have to configure the
		following Github Action Secrets in a <code>CI</code> environment in order for the Workflow to
		run through without errors. Example Values of these can be cound in the <code>.env.ci</code> file.
	</div>
</section>
