import type { ModalSettings } from '@skeletonlabs/skeleton';

export const logInModalConfig: ModalSettings = {
	type: 'component',
	component: 'logIn'
};

export const signUpModalConfig: ModalSettings = {
	type: 'component',
	component: 'signUp'
};
